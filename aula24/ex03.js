 // Criação de um objeto chamado "calculadora", com um método chamado "soma", que recebe dois parâmetros, e retorna a soma desses parâmetros.
 let calculadora = {

    soma (a,b){
        return a + b
    }
 }

 console.log(calculadora.soma(6,2))